package main

func main() {
	// fs := http.FileServer(http.Dir("./public"))
	// http.Handle("/", fs)

	// log.Print("Listening on :3001. . . .")
	// err := http.ListenAndServe(":3001", nil)
	// if err != nil {
	// 	log.Fatal(err)
	// }
}
